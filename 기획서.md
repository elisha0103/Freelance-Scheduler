# 프리랜서 스케줄러

## 0. 프로젝트 개요

### 0.1. 목적
프리랜서가 일정(시작일/마감일)과 수익·지출을 한 곳에서 관리할 수 있는 iOS 앱.

### 0.2. 기술 스택
- **플랫폼**: iOS 18.0+
- **UI 프레임워크**: SwiftUI
- **아키텍처**: MVVM
- **데이터 저장**: Firebase Firestore (원격 저장소)
- **인증**: Kakao SDK 단독 (Firebase Auth 미사용)
- **지도/장소 검색**: Naver Maps SDK, Naver Search API (지역 검색)
- **기타**: Swift Concurrency (async/await)

### 0.3. 로그인/회원가입
- 앱 최초 실행 시 로그인 화면 노출.
- **카카오 로그인** 지원 (Kakao SDK → Firebase Custom Token 연동).
- 회원가입 시 닉네임 설정 필수.
- 로그인 후 자동으로 그룹이 없으면 **그룹 생성 또는 초대코드 입력** 화면으로 이동.
- 그룹에 속하지 않으면 앱의 주요 기능 사용 불가 (그룹 필수).

---

## 1. 과제 목표

하단 3개의 탭으로 구성:

- **일정 탭**
    - 등록된 일정들을 기본으로는 캘린더 형태로 구성하고, 리스트로도 확인할 수 있는 탭.
    - 캘린더에는 해당일에 등록된 일정 혹은 가계부 항목을 최대 3개까지 노출하고, 4개 이상부터는 3번째 일정 표시란에 '3번째 일정 + N' 형식으로 표시한다. (N = 전체 항목 수 - 3)

- **가계부 탭**
    - 기본적으로는 리스트 형식으로 노출한다. 정렬은 등록된 가계부 일자의 최신순으로 정렬.
    - 가계부에는 일자를 기준으로 하단에 수입, 혹은 지출에 대한 금액이 있다.
    - 검색 아이콘과 통계 아이콘이 존재.

- **마이 페이지 탭**
    - 내가 속한 그룹 정보를 알 수 있음.
    - 그룹장은 한 그룹에만 속하고, 한 그룹에 최대 10명이 존재 가능.

---

## 2. 필수 구현 기능

### 2.1. 일정 탭 화면
- 기본 모드는 캘린더 형태이고, 해당 캘린더는 현재 일자의 월만 노출.
- 최상단 월 표시에 좌우 화살표로 월 이동 가능. 혹은 월을 탭하면 선택 다이얼이 나타나서 몇년도 몇월로 이동할건지 입력 가능한 알럿이 노출.
- 일자 칸에는 최대 3개의 일정 노출 가능. 일정은 한줄로만 표시되고, 카테고리별로 일정에 배경색이 존재함. 탭하면 해당 일자 상세페이지로 push 됨.
- 최상단 네비게이션 버튼 중 리스트 버튼을 누르면 풀스크린 모달로 리스트 형식의 일정 항목이 일렬로 나타남.
    - 리스트 화면에서는 일자 기준으로 하위에 해당 일자의 일정들이 정렬됨.
    - 리스트 화면에서도 마찬가지로 상단에 '몇년도 몇월'에 대한 내용이 있고 스크롤 하면서 상단 항목을 기준으로 '년도와 월'이 변경됨.
    - 단, 리스트 화면에서는 모든 일자를 노출하지 않고 일정이 등록된 '날짜'의 일정들만 리스팅됨.
    - 리스트 항목의 정렬은 일정이 존재하는 일자가 최신순으로 정렬.
- 최상단 네비게이션 버튼 중 + 버튼이 있고 누르면 팝오버로 일정 등록인지, 가계부 등록인지 선택 가능.
    - 일정 등록 선택 시 일정 등록 fullscreencover 창이 올라오고, 가계부는 가계부 등록 fullscreencover 창이 올라옴.

### 2.2. 일자 상세 화면
- 해당 일자 상세화면에서는 세 영역으로 구분됨.
    - **상단**: 일자 표시
    - **중간**: 해당 일자의 일정 항목들이 리스트 형식으로 나열
    - **하단**: 가계부 영역
- 해당 일정을 선택하면 일정의 상세페이지로 push 됨.
- 하단 가계부 영역에는 그 날에 등록된 수입, 지출 항목 표시. **수입은 초록 글씨**, **지출은 빨간 글씨**로 구분.
- 상단 네비게이션 우측에는 + 버튼이 있고 누르면 팝오버로 일정 등록인지, 가계부 등록인지 선택 가능.
- 일정 등록 선택 시 일정 등록 fullscreencover 창이 올라오고, 가계부는 가계부 등록 fullscreencover 창이 올라옴.
- **빈 상태**: 일정과 가계부가 모두 없으면 "등록된 항목이 없습니다" 안내 문구 노출.

### 2.3. 일정 상세 화면
- 해당 페이지에서는 다음 정보를 보여주는 화면:
    - 일정 제목
    - 일정 카테고리
    - 일정 시작 날짜와 시간
    - 일정 마감일
    - 일정 장소 주소
    - 일정 지도 (Naver Map)
    - 일정 금액
    - 일정 동행인
    - 설명(메모)
    - 그룹/개인 일정 여부 표시
    - 작성자 닉네임 (그룹 일정인 경우)
- '일정 장소 주소' 텍스트 옆에는 '공유' 버튼이 있어서, 이 버튼을 누르면 해당 위치를 '네이버 지도'로 바로 확인할 수 있는 딥링크 URL을 공유.
- **상단 네비게이션 우측에 편집 버튼** 존재. 탭하면 일정 편집 화면(등록 화면과 동일한 형태)으로 이동.
- **삭제 기능**: 편집 화면 하단에 '삭제' 버튼 존재. 탭하면 삭제 확인 알럿 노출 후 삭제.

### 2.4. 일정 등록/편집 화면
- 해당 페이지에서는 다음 항목을 작성한다:
    - 일정 제목 (필수, 최대 30자)
    - 일정 카테고리 선택 (필수)
    - 그룹 일정 여부 체크 (기본값: 개인 일정)
    - 일정 시작 날짜와 시간 (필수)
    - 일정 마감일 (선택)
    - 일정 장소 주소 (선택)
    - 일정 지도 (장소 선택 시 자동 표시)
    - 일정 금액 (선택, 숫자만 입력)
    - 일정 동행인 (선택, 같은 그룹 멤버 중 선택)
    - 설명 (선택, 최대 500자)
- 장소 텍스트 우측에 '검색' 버튼이 있고, 이 버튼을 누르면 모달로 네이버 API를 통해 장소 검색이 진행됨.
    - 장소를 검색하고 해당 장소를 리스트에서 탭하면 장소 선택이 되고 모달은 내려감.
- 선택된 장소를 기준으로 하단에 해당 장소에 대한 지도 정보가 표시됨.
- **동행인 선택**: 같은 그룹 멤버 목록에서 다중 선택 가능. 체크박스 형태.
- 등록/수정 버튼을 누르면 화면은 dismiss 됨.
- **필수 항목 미입력 시** 해당 필드에 빨간 테두리 + 안내 문구 노출.

### 2.5. 가계부 등록/편집 화면
- 가계부 맨 위에는 '제목' 입력란 (필수, 최대 30자).
- 그 아래에는 일자를 입력할 수 있음. 일자는 xxxx년 xx월 xx일 입력 가능하도록 DatePicker 사용.
- 그 다음, **수입 / 지출** 세그먼트 컨트롤로 선택.
    - **수입 선택 시**:
        - 금액 텍스트필드 (숫자 키패드). 오른쪽에 '세금' 체크박스.
        - 세금을 체크하면 '금액 - 세금' 아래 행에 '3.3%' 체크박스와 '직접 입력' 텍스트필드가 나타남.
        - '직접 입력' 텍스트필드를 탭하면 '3.3%'는 체크 해제됨.
        - '3.3%' 탭하면 해당 금액의 3.3%만큼 차감된 실수령액이 자동 계산되어 표시.
    - **지출 선택 시**:
        - 금액 텍스트필드만 노출 (숫자 키패드).
- 공통으로 맨 하단에는 '메모' 텍스트뷰 (줄바꿈 가능, 최대 200자).
- 맨 하단에 저장 버튼. 누르면 dismiss.
- **편집 모드 시** 하단에 '삭제' 버튼 추가. 삭제 확인 알럿 노출 후 삭제.
- **필수 항목 미입력 시** 해당 필드에 빨간 테두리 + 안내 문구 노출.

### 2.6. 일정 카테고리 정의
| 카테고리 | 배경색 |
|---------|--------|
| 미팅/회의 | 파란색 (#4A90D9) |
| 촬영/현장 | 주황색 (#F5A623) |
| 마감/납품 | 빨간색 (#D0021B) |
| 개인 | 초록색 (#7ED321) |
| 기타 | 회색 (#9B9B9B) |

> 일정 시작일은 해당 카테고리 배경색으로 표시, 일정 마감일은 **갈색 하이라이트(#8B572A)**로 표시.

---

## 3. 가계부 탭

### 3.1. 가계부 목록 화면
- 기본적으로는 리스트 형식으로 노출한다. 정렬은 등록된 가계부 일자의 최신순으로 정렬.
- 가계부 항목에는 일자를 기준으로 하단에 수입 혹은 지출에 대한 금액이 있다.
- 각 항목에는 제목, 일자, 금액(수입: 초록, 지출: 빨강), 세금 여부 표시.
- 항목을 탭하면 가계부 상세/편집 화면으로 이동.
- 가계부 탭 최상단 네비게이션바 버튼:
    - **좌측**: 검색 버튼
    - **우측**: 통계 버튼, + 버튼 (가계부 등록)
- **빈 상태**: 등록된 가계부가 없으면 "등록된 가계부가 없습니다" 안내 문구 노출.

### 3.2. 가계부 검색 화면
- 검색 버튼을 탭하면 상단에 검색바가 나타남.
- **검색 대상**: 가계부 제목, 메모 내용.
- **필터**: 수입/지출/전체 세그먼트 컨트롤.
- **기간 필터**: 시작일 ~ 종료일 선택 가능.
- 검색 결과는 리스트 형식으로 노출. 결과가 없으면 "검색 결과가 없습니다" 안내 문구 노출.

### 3.3. 통계 화면
- 통계 화면에서는 최초 현재 날짜를 기준으로 통계가 나타남.
- 화면 구성:
    - **상단**: N월 표시 (좌우 화살표로 월 이동 가능, N월 탭 시 직접 입력으로 특정 월 이동 알럿 노출)
    - **통계 항목** (리스트 형식):
        - 총 수입 금액
        - 총 지출 금액
        - 부과 세금 합계
        - N월 순이익 (수입 - 지출)

---

## 4. 마이페이지

### 4.1. 마이페이지 메인
- 본인 닉네임 + 편집 버튼
- 본인 그룹 이름
- 그룹장 여부 표시 (뱃지)
- 그룹장인 경우: **그룹 초대 버튼** 노출
- 그룹 관리 버튼
- 로그아웃 버튼

### 4.2. 로그아웃
- 로그아웃 시 한번 더 확인하는 알럿 노출.
- 확인 시 Kakao SDK 로그아웃 + Firebase 로그아웃 후 로그인 화면으로 이동.

### 4.3. 닉네임 편집 화면
- sheet로 나타남.
- 닉네임 입력 필드 (최소 2자, 최대 10자).
- 편집 완료 시 모든 화면에서 닉네임 표시가 수정됨.

### 4.4. 그룹 초대
- 그룹장이 '초대' 버튼을 탭하면 **초대 코드**(6자리 영숫자)가 생성됨.
- 초대 코드를 **클립보드 복사** 또는 **공유 시트**로 전달 가능.
- 초대 코드 유효기간: 24시간.
- 그룹원이 초대 코드를 입력하면 해당 그룹에 자동 합류 (최대 10명 제한 초과 시 알럿).

### 4.5. 그룹 관리 페이지
- **그룹장인 경우**:
    - 그룹 멤버 목록 노출 (닉네임, 가입일)
    - 각 멤버 옆에 '추방' 버튼. 탭 시 확인 알럿 후 추방.
    - 맨 하단에 '그룹 폐쇄' 버튼. 탭 시 확인 알럿 후 그룹 삭제 (모든 멤버 자동 탈퇴).
- **그룹원인 경우**:
    - 그룹 멤버 목록 노출 (닉네임, 가입일)
    - 맨 하단에 '그룹 탈퇴' 버튼. 탭 시 확인 알럿 후 탈퇴.

### 4.6. 그룹 생성
- 그룹에 속하지 않은 사용자가 '그룹 생성' 선택 시:
    - 그룹 이름 입력 (필수, 최대 20자)
    - 생성 시 해당 사용자가 자동으로 그룹장이 됨.

---

## 5. 데이터 모델

### 5.1. User (사용자)
| 필드 | 타입 | 설명 |
|------|------|------|
| id | String | Firebase UID |
| nickname | String | 닉네임 (2~10자) |
| email | String | 이메일 |
| groupId | String? | 소속 그룹 ID |
| isGroupLeader | Bool | 그룹장 여부 |
| createdAt | Date | 가입일 |

### 5.2. Group (그룹)
| 필드 | 타입 | 설명 |
|------|------|------|
| id | String | 그룹 고유 ID |
| name | String | 그룹 이름 |
| leaderId | String | 그룹장 User ID |
| memberIds | [String] | 멤버 User ID 목록 (최대 10명) |
| inviteCode | String? | 현재 유효한 초대 코드 |
| inviteCodeExpiry | Date? | 초대 코드 만료 시간 |
| createdAt | Date | 생성일 |

### 5.3. Schedule (일정)
| 필드 | 타입 | 설명 |
|------|------|------|
| id | String | 일정 고유 ID |
| title | String | 일정 제목 (최대 30자) |
| category | Category | 카테고리 (enum) |
| isGroupSchedule | Bool | 그룹 일정 여부 |
| startDate | Date | 시작 날짜/시간 |
| deadline | Date? | 마감일 |
| address | String? | 장소 주소 |
| latitude | Double? | 위도 |
| longitude | Double? | 경도 |
| amount | Int? | 금액 |
| companions | [String] | 동행인 User ID 목록 |
| description | String? | 설명 (최대 500자) |
| authorId | String | 작성자 User ID |
| groupId | String | 소속 그룹 ID |
| createdAt | Date | 등록일 |

### 5.4. AccountBook (가계부)
| 필드 | 타입 | 설명 |
|------|------|------|
| id | String | 고유 ID |
| title | String | 제목 (최대 30자) |
| date | Date | 가계부 일자 |
| type | IncomeExpense | 수입/지출 (enum) |
| amount | Int | 금액 |
| hasTax | Bool | 세금 여부 |
| taxRate | Double? | 세율 (3.3% 또는 직접 입력) |
| taxAmount | Int? | 세금 금액 |
| netAmount | Int? | 실수령액 (수입 - 세금) |
| memo | String? | 메모 (최대 200자) |
| authorId | String | 작성자 User ID |
| groupId | String | 소속 그룹 ID |
| createdAt | Date | 등록일 |

---

## 6. 기타 규칙

### 6.1. 그룹과 데이터 공유
- 그룹의 일정은 '그룹 일정'만 공유되고 '개인 일정'은 공유되지 않음.
- 같은 그룹의 A와 B가 있다면, 각자가 등록한 그룹 일정은 A와 B 모두에게 공유되고, 개인 일정은 각자만 확인 가능.
- **가계부 정보는 그룹에 귀속됨.** 개인 가계부 설정 불가. 같은 그룹 멤버 모두 가계부 열람 가능.

### 6.2. 일정 표시 규칙
- 일정 생성 시, 일정 탭에서는 해당 일정의 '일정 시작'과 '일정 마감' 두 개의 일정이 모두 표시 됨.
- **일정 시작은 카테고리 배경색**으로, **일정 마감은 갈색 하이라이트(#8B572A)**로 표시.
- 마감일이 없는 일정은 시작일만 표시.

### 6.3. 빈 상태 (Empty State)
- 각 화면에서 데이터가 없을 때 적절한 안내 문구와 아이콘 노출.
- 예: "등록된 일정이 없습니다", "등록된 가계부가 없습니다", "검색 결과가 없습니다"

### 6.4. 알림 (v2 고려)
- 1차 버전에서는 알림 기능 미구현.
- 향후 일정 시작 전 알림, 마감일 임박 알림 등 추가 고려.
